# Agent Correction Execution Plan

Inherit the parent approval, dispatch, Actions-only, commit, and finish gates.
Execute after terminal navigation establishes flat-tab selection with unchanged
runtime identities. Main owns spec changes; implement/check agents do not recurse.

## Implementation Order

- [ ] Read the evidence report and runtime/reconciliation/inventory/context specs.
- [ ] Add bounded lineage/foreground identity facts and logical launcher
  normalization inside the existing inventory ownership boundary.
- [ ] Separate process liveness from run-owned semantic activity. Remove the
  unconditional shared-output Working/Waiting assignment and project only
  accepted, owned observations.
- [ ] Keep automatic catalog progress quiet without suppressing manual progress,
  errors, or last-known/stale handling.
- [ ] Add exact Runtime title projection and distinguishable fallback; retain
  provider/session ownership gates and compact presentation.
- [ ] Update focused regressions and request a Trellis check review through main.

## Actions-Only Cases

- Empty/no-Agent terminals across repeated automatic/manual refresh and failure.
- Real work, input wait, approval wait, completion, unrelated shell output,
  redraw, and missing/expired semantic evidence.
- Owned background worktrees versus unrelated external terminal processes;
  copied route, parent/child launchers, independent runs, same path, and old epoch.
- Two-empty retirement, recreated poll state, stale output after exit, genuine
  subsequent launch, and disconnected last-known state.
- Shared SSH labels, exact/missing/mismatched session IDs, manual names, and
  legacy grouped terminals now exposed as flat tabs.

Run these inside existing Actions test jobs, including generated probe execution
on disposable runner processes only. Preserve the public tracker input API in
cross-crate tests. Record exact SHA and native artifact acceptance separately.

## Risky Surfaces

`mt-ai` runtime/tracker, `mt-ssh` inventory, `mt-pty` SSH route launch if required,
`mt-app` remote polling/accepted projection/context/Sessions/sidebar/catalog.
Do not trade identity fences or global animation suppression for a quiet UI.
